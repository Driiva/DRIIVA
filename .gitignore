node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Cursor IDE / agent
.cursor/

# Environment files (may contain secrets)
.env
.env.local
.env.*.local
.env.vercel*
functions/.env
functions/.env.local
functions/.env.driiva-staging
.vercel
api/_server.js
.env.staging

# Firestore backup (local export; may contain PII)
firestore-backup/

# Firebase CLI
firebase-debug.log
firebase-debug.*.log
firestore-debug.log
ui-debug.log

# Repo bloat — assets, archives, experimental
attached_assets/
*.zip
coverage/
claude-sentinel/
Driiva Marketing/

# Markdown code dumps (not documentation)
ANTIGRAVITY_AGENTS_DRIIVA.md
DRIVING_BEHAVIOR_PROCESSING.md
RUNBOOK.md

# Local screenshots + tooling caches
/driiva-*.png
.playwright-mcp/
driiva-design-system/
CONTEXT.md

# Overnight build run logs (transient, not artifacts)
.overnight-logs/
apps/*/lighthouse.json

# Playwright (characterisation E2E)
playwright-report/
test-results/
dev-server.log

# Subagent-driven-development scratch (ledger + task briefs/reports)
.superpowers/

# Business/confidential assets merged in from the old ~/Documents/Driiva
# folder (deck, financials, legal, investor material) - never code, never tracked
/Branding/
/Conceptual Framework/
/Driiva HyperFrames/
/Driiva Stages/
/Driiva-Deck.pptx
/Finance/
/Investor Decks/
/Legal + Compliance/
/Marketing/
/Misc\ /
/MVP/
/Research/

# SSR build artefact for prerendering — regenerated by npm run build, not tracked
apps/marketing/dist-ssr/

# Agent worktrees are local scratch, never repo content
.claude/worktrees/

# verify:beta writes an Expo bundle here to prove the app builds
.beta-export/
